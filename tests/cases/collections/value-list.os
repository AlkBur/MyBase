	НомерТеста = 0;
	
	ТестДолжен_ПроверитьСозданиеСпискаЗначений();
	ТестДолжен_ПроверитьДобавлениеЭлементов();
	ТестДолжен_ПроверитьДоступКСвойствамЭлементаСписка();
	ТестДолжен_ПроверитьЗаписьСвойствЭлементаСписка();
	ТестДолжен_ПроверитьИтерациюПоСписку();
	ТестДолжен_ПроверитьДоступПоИндексу();

	ТестДолжен_ПроверитьМетодИндексСпискаЗначений();
	ТестДолжен_ПроверитьМетодВставитьСпискаЗначений();
	ТестДолжен_ПроверитьМетодСдвинутьСпискаЗначений();
	ТестДолжен_ПроверитьМетодСортироватьПоЗначениюСпискаЗначений();
	ТестДолжен_ПроверитьМетодСортироватьПоПредставлениюСпискаЗначений();
	ТестДолжен_ПроверитьМетодНайтиСпискаЗначений();
	ТестДолжен_ПроверитьМетодУдалитьСпискаЗначений();
	ТестДолжен_ПроверитьПреобразованиеЭлементаСпискаВСтроку();
	ТестДолжен_ПроверитьМутациюЧерезИндекс();
	ТестДолжен_ПроверитьСтабильнуюСортировку();
	ТестДолжен_ПроверитьУдалитьСтрока01();
	ТестДолжен_ПроверитьТипЗнчКартинка();


&НаКлиенте
Процедура ТестДолжен_ПроверитьСозданиеСпискаЗначений() Экспорт
	СЗ = Новый СписокЗначений;
	ПроверитьРавенство(Тип("СписокЗначений"), ТипЗнч(СЗ));
КонецПроцедуры

&НаКлиенте
Процедура ТестДолжен_ПроверитьДобавлениеЭлементов() Экспорт
	СЗ = Новый СписокЗначений;
	СЗ.Добавить("Один");
	СЗ.Добавить("Два");
	ПроверитьРавенство(2, СЗ.Количество());
	ПроверитьРавенство(Тип("ЭлементСпискаЗначений"), ТипЗнч(СЗ.Получить(0)));
КонецПроцедуры

&НаКлиенте
Процедура ТестДолжен_ПроверитьДоступКСвойствамЭлементаСписка() Экспорт
	
	СЗ = Новый СписокЗначений;
	СЗ.Добавить(1,"Представление");
	СЗ.Добавить(2,"Представление2", Истина);
	СЗ.Добавить(3,"Представление3", Истина, Новый Картинка);
	СЗ.Добавить(1);
	
	Элемент = СЗ[0];
	ПроверитьРавенство(1, Элемент.Значение, "Значение элемента 0 должно быть установлено");
	ПроверитьРавенство("Представление", Элемент.Представление, "Представление элемента 0 должно быть установлено");
	ПроверитьРавенство(Ложь, Элемент.Пометка, "Пометка элемента 0 не должна быть установлена");
	ПроверитьРавенство(Неопределено, Элемент.Картинка, "Картинка элемента 0 не должна быть установлена");
	
	Элемент = СЗ[1];
	ПроверитьРавенство(2, Элемент.Значение, "Значение элемента 1 должно быть установлено");
	ПроверитьРавенство("Представление2", Элемент.Представление, "Представление элемента 1 должно быть установлено");
	ПроверитьРавенство(Истина, Элемент.Пометка, "Пометка элемента 1 должна быть установлена");
	
	Элемент = СЗ[2];
	ПроверитьРавенство(3, Элемент.Значение, "Значение элемента 2 должно быть установлено");
	ПроверитьРавенство("Представление3", Элемент.Представление, "Представление элемента 2 должно быть установлено");
	ПроверитьРавенство(Истина, Элемент.Пометка, "Пометка элемента 2 должна быть установлена");
	ПроверитьРавенство("Тут должна быть картинка, но сейчас может быть любое значение", Элемент.Картинка, "Картинка элемента 2 должна быть установлена");
	
	ПроверитьРавенство("", СЗ[3].Представление, "Должно быть пустое представление по умолчанию");
	
КонецПроцедуры

&НаКлиенте
Процедура ТестДолжен_ПроверитьЗаписьСвойствЭлементаСписка() Экспорт
	СЗ = Новый СписокЗначений;
	Элемент = СЗ.Добавить(1);
	
	Элемент.Значение = 2;
	Элемент.Представление = "Привет";
	Элемент.Пометка = Истина;
	Элемент.Картинка = Новый Картинка;
	
	ПроверитьРавенство(2, Элемент.Значение, "Значение элемента должно быть установлено");
	ПроверитьРавенство("Привет", Элемент.Представление, "Представление элемента должно быть установлено");
	ПроверитьРавенство(Истина, Элемент.Пометка, "Пометка элемента должна быть установлена");
	ПроверитьРавенство("---", Элемент.Картинка, "Картинка элемента должна быть установлена");
	
КонецПроцедуры

&НаКлиенте
Процедура ТестДолжен_ПроверитьИтерациюПоСписку() Экспорт
	СЗ = Новый СписокЗначений;
	СЗ.Добавить(1);
	СЗ.Добавить(2);
	СЗ.Добавить(3);
	СЗ.Добавить(4);
	Сч = 0;
	Для Каждого Элемент Из СЗ Цикл
		Сч = Сч + 1;
	КонецЦикла;
	
	ПроверитьРавенство(4, Сч);
	
КонецПроцедуры

&НаКлиенте
Процедура ТестДолжен_ПроверитьДоступПоИндексу() Экспорт
	СЗ = Новый СписокЗначений;
	СЗ.Добавить(1);
	
	ПроверитьРавенство(1, СЗ[0].Значение);
	Попытка
		СЗ[0] = СЗ[0];
	Исключение
		ПроверитьРавенство("Индексированное значение доступно только для чтения", ИнформацияОбОшибке().Описание);
		Возврат;
	КонецПопытки;
	
	ВызватьИсключение "Должно было быть выброшено исключение, но это не произошло";
	
КонецПроцедуры

&НаКлиенте
Процедура ТестДолжен_ПроверитьМетодИндексСпискаЗначений() Экспорт

	СЗ = Новый СписокЗначений;

	СЗ.Добавить(1);
	СЗ.Добавить(2);
	СЗ.Добавить(3);
	СЗ.Добавить(4);

	ПроверитьРавенство(СЗ.Индекс(СЗ[0]), 0, "Метод Индекс() списка значений");
	ПроверитьРавенство(СЗ.Индекс(СЗ[1]), 1, "Метод Индекс() списка значений");
	ПроверитьРавенство(СЗ.Индекс(СЗ[2]), 2, "Метод Индекс() списка значений");
	ПроверитьРавенство(СЗ.Индекс(СЗ[3]), 3, "Метод Индекс() списка значений");

	ДругойСЗ = Новый СписокЗначений;
	ДругойСЗ.Добавить(1);

	ПроверитьРавенство(СЗ.Индекс(ДругойСЗ[0]), -1, "Метод Индекс() списка значений: несуществующий элемент");

КонецПроцедуры

&НаКлиенте
Процедура ТестДолжен_ПроверитьМетодВставитьСпискаЗначений() Экспорт

	СЗ = Новый СписокЗначений;

	Э3 = СЗ.Вставить(0, 3); // Вставка в пустой список
	Э1 = СЗ.Вставить(0, 1); // Вставка в начало
	Э2 = СЗ.Вставить(1, 2); // Вставка в середину
	Э4 = СЗ.Вставить(3, 4); // Вставка в конец

	ПроверитьРавенство(СЗ.Индекс(Э1), 0, "Метод Вставить() списка значений");
	ПроверитьРавенство(СЗ.Индекс(Э2), 1, "Метод Вставить() списка значений");
	ПроверитьРавенство(СЗ.Индекс(Э3), 2, "Метод Вставить() списка значений");
	ПроверитьРавенство(СЗ.Индекс(Э4), 3, "Метод Вставить() списка значений");

КонецПроцедуры

&НаКлиенте
Процедура ТестДолжен_ПроверитьМетодУдалитьСпискаЗначений() Экспорт

	СЗ = Новый СписокЗначений;
	Для Инд = 1 По 5 Цикл
		СЗ.Добавить(Инд);
	КонецЦикла;
	Э1 = СЗ[0];
	Э2 = СЗ[1];
	Э3 = СЗ[2];
	Э4 = СЗ[3];
	Э5 = СЗ[4];

	СЗ.Удалить(1);
	ПроверитьРавенство(СЗ.Количество(),4, "Удаление по индексу");
	ПроверитьРавенство(СЗ.Индекс(Э2),-1, "Удаление по индексу");
	ПроверитьРавенство(СЗ.Индекс(Э4),2, "Удаление по индексу");

	СЗ.Удалить(Э1);
	ПроверитьРавенство(СЗ.Количество(),3, "Удаление элемента");
	ПроверитьРавенство(СЗ.Индекс(Э1),-1, "Удаление элемента");
	ПроверитьРавенство(СЗ.Индекс(Э4),1, "Удаление элемента");

	СЗ.Удалить("1");
	ПроверитьРавенство(СЗ.Количество(),2, "Удаление по индексу типа строка");
	ПроверитьРавенство(СЗ.Индекс(Э4),-1, "Удаление по индексу типа строка");
	ПроверитьРавенство(СЗ.Индекс(Э5),1, "Удаление по индексу типа строка");

	// Проверим ошибки
	Ошибка = "Удаление с неверным индексом";
	Попытка
		СЗ.Удалить(10);
	Исключение
		Ошибка = ИнформацияОбОшибке().Описание;
	КонецПопытки;
	ПроверитьРавенство(Ошибка, "Значение индекса выходит за пределы диапазона");

	Ошибка = "Удаление несуществующего элемента";
	ДругойСЗ = Новый СписокЗначений;
	ДругойСЗ.Добавить(1);
	Попытка
		СЗ.Удалить(ДругойСЗ[0]);
	Исключение
		Ошибка = ИнформацияОбОшибке().Описание;
	КонецПопытки;
	ПроверитьРавенство(Ошибка, "Элемент не принадлежит списку значений");

	Ошибка = "Удаление с неверным типом параметра";
	Попытка
		СЗ.Удалить("ё");
	Исключение
		Ошибка = ИнформацияОбОшибке().Описание;
	КонецПопытки;
	ПроверитьРавенство(Ошибка, "Неверный тип аргумента");

КонецПроцедуры


&НаКлиенте
Функция ПроверитьПорядок(Знач СЗ, Знач П1,
	Знач П2 = Неопределено,
	Знач П3 = Неопределено,
	Знач П4 = Неопределено,
	Знач П5 = Неопределено)

	Массив = Новый Массив;
	Массив.Добавить(П1);

	Если П2 <> Неопределено Тогда
		Массив.Добавить(П2);
	КонецЕсли;
	Если П3 <> Неопределено Тогда
		Массив.Добавить(П3);
	КонецЕсли;
	Если П4 <> Неопределено Тогда
		Массив.Добавить(П4);
	КонецЕсли;
	Если П5 <> Неопределено Тогда
		Массив.Добавить(П5);
	КонецЕсли;

	Для Инд = 0 По Массив.ВГраница() Цикл

		Если СЗ[Инд].Значение <> Массив[Инд] Тогда
			Возврат Ложь;
		КонецЕсли;

	КонецЦикла;

	Возврат Истина;

КонецФункции

&НаКлиенте
Процедура ТестДолжен_ПроверитьМетодСдвинутьСпискаЗначений() Экспорт

	СЗ = Новый СписокЗначений;

	Для Инд = 1 По 5 Цикл
		СЗ.Добавить(Инд);
	КонецЦикла;

	С1 = СЗ[0];
	С2 = СЗ[1];
	С3 = СЗ[2];
	С4 = СЗ[3];
	С5 = СЗ[4];

	// Проверим обычное перемещение
	СЗ.Сдвинуть(С2, -1);
	ПроверитьИстину(ПроверитьПорядок(СЗ, 2, 1, 3, 4, 5), "Сдвиг вверх");

	СЗ.Сдвинуть(С1, 2);
	ПроверитьИстину(ПроверитьПорядок(СЗ, 2, 3, 4, 1, 5), "Сдвиг вниз");

	СЗ.Сдвинуть(3, -2);
	ПроверитьИстину(ПроверитьПорядок(СЗ, 2, 1, 3, 4, 5), "Сдвиг по индексу вверх");

	СЗ.Сдвинуть(1, 2);
	ПроверитьИстину(ПроверитьПорядок(СЗ, 2, 3, 4, 1, 5), "Сдвиг по индексу вниз");

	// Проверим приведение типа
	СЗ.Сдвинуть("2", 2);
	ПроверитьИстину(ПроверитьПорядок(СЗ, 2, 3, 1, 5, 4), "Сдвиг, индекс - строка");

	СЗ.Сдвинуть("1", "2");
	ПроверитьИстину(ПроверитьПорядок(СЗ, 2, 1, 5, 3, 4), "Сдвиг, индекс и смещение - строки");

	// Проверим крайние случаи
	СЗ.Сдвинуть(0, 4);
	ПроверитьИстину(ПроверитьПорядок(СЗ, 1, 5, 3, 4, 2), "Сдвиг по индексу вниз");

	СЗ.Сдвинуть(4, -4);
	ПроверитьИстину(ПроверитьПорядок(СЗ, 2, 1, 5, 3, 4), "Сдвиг по индексу вниз");

	// Проверим ошибки
	Ошибка = "Сдвиг за пределы вниз";
	Попытка
		СЗ.Сдвинуть(0, 5);
	Исключение
		Ошибка = ИнформацияОбОшибке().Описание;
	КонецПопытки;
	ПроверитьРавенство(Ошибка, "Неверное значение аргумента номер 2");

	Ошибка = "Сдвиг за пределы вверх";
	Попытка
		СЗ.Сдвинуть(1, -3);
	Исключение
		Ошибка = ИнформацияОбОшибке().Описание;
	КонецПопытки;
	ПроверитьРавенство(Ошибка, "Неверное значение аргумента номер 2");

	Ошибка = "Сдвиг с неверным индексом";
	Попытка
		СЗ.Сдвинуть(10, 2);
	Исключение
		Ошибка = ИнформацияОбОшибке().Описание;
	КонецПопытки;
	ПроверитьРавенство(Ошибка, "Значение индекса выходит за пределы диапазона");

	Ошибка = "Сдвиг несуществующего элемента";
	ДругойСЗ = Новый СписокЗначений;
	ДругойСЗ.Добавить(1);
	Попытка
		СЗ.Сдвинуть(ДругойСЗ[0], 2);
	Исключение
		Ошибка = ИнформацияОбОшибке().Описание;
	КонецПопытки;
	ПроверитьРавенство(Ошибка, "Элемент не принадлежит списку значений");

	Ошибка = "Сдвиг с неверным типом параметра";
	Попытка
		СЗ.Сдвинуть("ё", 2);
	Исключение
		Ошибка = ИнформацияОбОшибке().Описание;
	КонецПопытки;
	ПроверитьРавенство(Ошибка, "Неверный тип аргумента");
	
КонецПроцедуры

&НаКлиенте
Процедура ТестДолжен_ПроверитьМетодСортироватьПоЗначениюСпискаЗначений() Экспорт

	СЗ = Новый СписокЗначений;
	СЗ.Добавить(4);
	СЗ.Добавить(3);
	СЗ.Добавить(2);
	СЗ.Добавить(1);

	СЗ.СортироватьПоЗначению();
	ПроверитьИстину(ПроверитьПорядок(СЗ, 1, 2, 3, 4), "Сортировка по-умолчанию");

	СЗ.СортироватьПоЗначению(НаправлениеСортировки.Убыв);
	ПроверитьИстину(ПроверитьПорядок(СЗ, 4, 3, 2, 1), "Сортировка по убыванию");

	СЗ.СортироватьПоЗначению(НаправлениеСортировки.Возр);
	ПроверитьИстину(ПроверитьПорядок(СЗ, 1, 2, 3, 4), "Сортировка по возрастанию");
	
	СЗ = Новый СписокЗначений;
	СЗ.Добавить("аааааб");
	СЗ.Добавить("аааааа");
	СЗ.Добавить("Бааааа");
	
	СЗ.СортироватьПоЗначению();
	ПроверитьИстину(ПроверитьПорядок(СЗ, "аааааа", "аааааб", "Бааааа"), "Сортировка по регистру");

КонецПроцедуры


&НаКлиенте
Процедура ТестДолжен_ПроверитьМетодСортироватьПоПредставлениюСпискаЗначений() Экспорт

	СЗ = Новый СписокЗначений;
	СЗ.Добавить(4,"14");
	СЗ.Добавить(3,"3");
	СЗ.Добавить(2,"2");
	СЗ.Добавить(1,"1");

	СЗ.СортироватьПоПредставлению();
	ПроверитьИстину(ПроверитьПорядок(СЗ, 1, 4, 2, 3), "Сортировка по-умолчанию");

	СЗ.СортироватьПоПредставлению(НаправлениеСортировки.Убыв);
	ПроверитьИстину(ПроверитьПорядок(СЗ, 3, 2, 4, 1), "Сортировка по убыванию");

	СЗ.СортироватьПоПредставлению(НаправлениеСортировки.Возр);
	ПроверитьИстину(ПроверитьПорядок(СЗ, 1, 4, 2, 3), "Сортировка по возрастанию");

КонецПроцедуры

&НаКлиенте
Процедура ТестДолжен_ПроверитьМетодНайтиСпискаЗначений() Экспорт

	СЗ = Новый СписокЗначений;
	Э0 = СЗ.Добавить(1.2);
	Э1 = СЗ.Добавить("абв");
	Э2 = СЗ.Добавить('20161009');
	Э3 = СЗ.Добавить(Истина);
	Э4 = СЗ.Добавить(Null);
	Э5 = СЗ.Добавить(Неопределено);

	ПроверитьРавенство(СЗ.НайтиПоЗначению(1.2)         , Э0, "Поиск числа");
	ПроверитьРавенство(СЗ.НайтиПоЗначению("абв")       , Э1, "Поиск строки");
	ПроверитьРавенство(СЗ.НайтиПоЗначению("АБВ")       , Неопределено, "Поиск строки в другом регистре");
	ПроверитьРавенство(СЗ.НайтиПоЗначению('20161009')  , Э2, "Поиск даты");
	ПроверитьРавенство(СЗ.НайтиПоЗначению(Истина)      , Э3, "Поиск Истины");
	ПроверитьРавенство(СЗ.НайтиПоЗначению(Null)        , Э4, "Поиск Null");
	ПроверитьРавенство(СЗ.НайтиПоЗначению(Неопределено), Э5, "Поиск Неопределено");

КонецПроцедуры

&НаКлиенте
Процедура ТестДолжен_ПроверитьПреобразованиеЭлементаСпискаВСтроку() Экспорт
	СЗ = Новый СписокЗначений;
	СЗ.Добавить(0);
	СЗ.Добавить(1, "");
	СЗ.Добавить(2, Неопределено);
	СЗ.Добавить(3, " ");

	ПроверитьРавенство("0", Строка(СЗ[0]), "Строка, без указания");
	ПроверитьРавенство("", СЗ[0].Представление, "Представление, без указания");
	ПроверитьРавенство("1", Строка(СЗ[1]), "Строка, с пустой");
	ПроверитьРавенство("", СЗ[1].Представление, "Представление, с пустой");
	ПроверитьРавенство("2", Строка(СЗ[2]), "Строка, Неопределено");
	ПроверитьРавенство("", СЗ[2].Представление, "Представление, Неопределено");
	ПроверитьРавенство(" ", Строка(СЗ[3]), "Строка, пробел");
	ПроверитьРавенство(" ", СЗ[3].Представление, "Представление, пробел");

КонецПроцедуры

&НаКлиенте
Процедура ТестДолжен_ПроверитьМутациюЧерезИндекс() Экспорт
	СЗ = Новый СписокЗначений;
	СЗ.Добавить(1);
	СЗ[0].Значение = 123;
	ПроверитьРавенство(123, СЗ.Получить(0).Значение, "Мутация через индекс — identity semantics");
КонецПроцедуры

&НаКлиенте
Процедура ТестДолжен_ПроверитьСтабильнуюСортировку() Экспорт
	// Stable sort invariant: при равных Значениях порядок Представлений сохраняется
	СЗ = Новый СписокЗначений;
	СЗ.Добавить(1, "A");
	СЗ.Добавить(1, "B");
	СЗ.СортироватьПоЗначению();
	ПроверитьРавенство("A", СЗ[0].Представление, "Stable sort: первый элемент");
	ПроверитьРавенство("B", СЗ[1].Представление, "Stable sort: второй элемент");
КонецПроцедуры

&НаКлиенте
Процедура ТестДолжен_ПроверитьУдалитьСтрока01() Экспорт
	// Intentional coercion: строки-цифры кастуются к Number
	СЗ = Новый СписокЗначений;
	СЗ.Добавить("ноль");
	СЗ.Добавить("один");
	СЗ.Удалить("01"); // index 1 (не значение "01")
	ПроверитьРавенство(1, СЗ.Количество(), "Удалить('01') удаляет по индексу 1");
	ПроверитьРавенство("ноль", СЗ[0].Значение, "Удалить('01') — остался нулевой элемент");
КонецПроцедуры

&НаКлиенте
Процедура ТестДолжен_ПроверитьТипЗнчКартинка() Экспорт
	Карт = Новый Картинка;
	ПроверитьРавенство(Тип("Картинка"), ТипЗнч(Карт), "ТипЗнч(Новый Картинка)");
	ПроверитьРавенство("Картинка", Строка(Карт), "Строка(Новый Картинка)");
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьРавенство(Значение1, Значение2, Знач Комментарий="")
	Если Не ПустаяСтрока(Комментарий) Тогда
		Комментарий = " ("+Комментарий+")";
	КонецЕсли;
	ТексДляШаблона = "%1) %2 %3 %4%5";	
	
	
	НомерТеста = НомерТеста + 1;
	Если Значение1 = Значение2 Тогда
		СообщитьJSON(СтрШаблон(ТексДляШаблона, НомерТеста, Значение1, "=",
			Значение2, Комментарий));
	Иначе
		СообщитьJSON(СтрШаблон(ТексДляШаблона, НомерТеста, Значение1, "<>",
			Значение2, Комментарий));
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьИстину(Значение1, Знач Комментарий="")
	ПроверитьРавенство(Истина, Значение1, Комментарий);
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьЛожь(Значение1, Знач Комментарий="")
	ПроверитьРавенство(Ложь, Значение1, Комментарий);
КонецПроцедуры


&НаКлиентеНаСервереБезКонтекста
Процедура СообщитьJSON(Текст)
	Сообщить(Текст);
КонецПроцедуры
